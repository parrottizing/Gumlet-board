# LAN Clipboard Protocol Docs

This folder contains the shared protocol and data contract for FlorisBoard <-> macOS LAN clipboard sync.

## Versions

- `v1/`:
  - Phase 1 protocol contract for text sync (v1 deliverable).
  - Includes v2-reserved image payload contract so both implementations can keep one forward-compatible parser.

